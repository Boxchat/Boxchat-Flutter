import 'package:flutter/material.dart';
class RouterInvite extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("邀请朋友"),
      ),
      body: Center(
        child: Text("This is new route"),
      ),
    );
  }
}