import 'package:flutter/material.dart';
class RouterNewRadio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("新建电台"),
      ),
      body: Center(
        child: Text("This is new route"),
      ),
    );
  }
}