import 'package:flutter/material.dart';
class RouterStatement extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("软件声明"),
      ),
      body: Center(
        child: Text("This is new route"),
      ),
    );
  }
}