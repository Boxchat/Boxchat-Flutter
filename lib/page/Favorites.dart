import 'package:flutter/material.dart';
class RouterFavorites extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("收藏夹"),
      ),
      body: Center(
        child: Text("This is new route"),
      ),
    );
  }
}